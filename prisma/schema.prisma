// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder model to allow Prisma client generation
// Real models will be added in the entity implementation phase
model Placeholder {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
}

// TODO: Add all 26 entity models based on database schema
// Entities will be added in priority order:
// 1. Actors: Guest, User, Staff, Operator, Admin, Partner
// 2. Assets: Apartment, Room
// 3. Contracts: RentalContract, UserContractMember
// 4. Requests: ContactRequest, BookingRequest, Appointment, PartnerRequest
// 5. Operations: Task, MaintenanceRequest, Ticket
// 6. Financial: Invoice, Payment
// 7. IoT: IoTDevice, UtilityMeter, UtilityReading
// 8. Documents: Policy, LegalDocument
// 9. Audit: ActivityLog, Notification
